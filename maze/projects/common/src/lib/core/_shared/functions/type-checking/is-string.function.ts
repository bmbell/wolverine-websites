/**
 * Determines if the reference is a string
 * @param value - The reference to check
 * @returns True if the reference is a string; otherwise false
 */
export function isString(value: any): boolean {
    return typeof value === "string";
}
