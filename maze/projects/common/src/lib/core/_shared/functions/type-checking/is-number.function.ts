/**
 * Determines if the reference is a number
 * @param value - The reference to check
 * @returns True if the reference is a number; otherwise false
 */
export function isNumber(value: any): boolean {
    return typeof value === "number";
}
