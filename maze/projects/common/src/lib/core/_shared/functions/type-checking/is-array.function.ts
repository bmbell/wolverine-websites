/**
 * Determines if the reference is an array
 * @param value - The reference to check
 * @returns True if the reference is an array; otherwise false
 */
export function isArray(obj: any): boolean {
    return Array.isArray(obj);
}
